// @dart=2.12
import 'package:flutter/material.dart';

class ErrorCustom extends StatelessWidget {
  const ErrorCustom({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Error Occured"),
    );
  }
}
